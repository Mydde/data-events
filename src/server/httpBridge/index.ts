'use strict'


import * as http from 'http';
import {httpRoutes} from './httpRoutes';
import {httpHandler} from './httpHandler';

export {
  http,
  httpHandler,
  httpRoutes
}

module.exports = {
	http,
	httpHandler,
	httpRoutes
}